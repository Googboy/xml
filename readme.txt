今天主要完成的是代码中操作XML数据，以及httpURLconnection文件下载功能。
其中遇到的问题主要有：httpURLconnection中我是在eclipse中Tomcat服务上部署了一个web项目，
如果不运行起来的话，AS中是访问不到图片资源的。
（异常信息为:java.net.ConnectException: failed to connect to /192.168.12.100 (port 8080) after 8000ms: isConnected failed: ECONNREFUSED (Connection refused)）
